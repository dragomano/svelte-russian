---
title: Создание проекта
origin: https://svelte.dev/docs/kit/creating-a-project
sidebar:
  order: 1
---

Самый простой способ начать создавать приложение на SvelteKit — выполнить команду:

import { Tabs, TabItem } from '@astrojs/starlight/components';

<Tabs syncKey="starlight-package-managers-pkg">
  <TabItem icon="seti:npm" label="npm">
    ```sh
    npx sv create my-app
    cd my-app
    npm run dev
    ```
  </TabItem>
  <TabItem icon="pnpm" label="pnpm">
    ```sh
    pnpx sv create myapp
    cd myapp
    pnpm run dev
    ```
  </TabItem>
  <TabItem icon="seti:yarn" label="yarn">
    ```sh
    yarn dlx sv create myapp
    cd myapp
    yarn dev
    ```
  </TabItem>
</Tabs>

Первая команда создаст новый проект в директории `my-app`, предложив настроить некоторые базовые инструменты, такие как TypeScript. См. [документацию CLI](/cli/introduction/overview) для получения информации об этих опциях и [страницу интеграций](https://svelte.dev/docs/kit/integrations) для рекомендаций по настройке дополнительных инструментов. Команда `npm run dev` запустит сервер разработки на `http://localhost:5173` — убедитесь, что вы установили зависимости перед выполнением этой команды, если не сделали этого во время создания проекта.

Есть две базовые концепции:

- Каждая страница вашего приложения — это [компонент Svelte](/introduction/overview)
- Страницы создаются путём добавления файлов в директорию `src/routes` вашего проекта. Они будут рендериться на сервере, чтобы первая загрузка приложения была максимально быстрой, после чего управление переходит клиентскому приложению

Попробуйте отредактировать файлы, чтобы понять, как всё работает.

## Настройка редактора

Мы рекомендуем использовать [Visual Studio Code (VS Code)](https://code.visualstudio.com/download) с [расширением Svelte](https://marketplace.visualstudio.com/items?itemName=svelte.svelte-vscode), но [также есть поддержка для многих других редакторов](https://sveltesociety.dev/collection/editor-support-c85c080efc292a34).
